%funcion que devuelve parametros de evaluacion a partir de los valores de la matriz de confusion


function [Accuracy,Precision,Recall,F_measure] = accuracy_num(FP,TP,N,P)

Accuracy = (TP + FP) / (P + N);
Precision = TP / (TP + FP);
Recall = TP / P;
F_measure = (2*Precision * Recall)/(Precision + Recall);

end 
